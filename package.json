{
  "name": "aseed-atelier",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": "^24.15.0"
  },
  "devDependencies": {
    "jsdom": "30.0.1"
  }
}
